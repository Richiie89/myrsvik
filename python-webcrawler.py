#!/usr/bin/env python
import threading
import socket
import subprocess
import sys
import os
import nmap
from datetime import datetime
#Clear the screen#
subprocess.call('clear', shell=True)
#
#
print "RUNNING STAGE1 (port scanning, this will take a while)"
with open(whois-netherlands-full.txt) as f:
for line in f:
print(line)
